src
 ── app
     ── (public)
         ── about-us
             ── AboutContent.jsx
             ── page.jsx
         ── aplicar
             ── AplicarContent.jsx
             ── page.jsx
             ── trabajos
         ── apply
             ── ApplyContent.jsx
             ── jobs
             ── page.jsx
         ── booking
             ── BookingContent.jsx
             ── confirmation
             ── page.jsx
         ── cleaning-tips
             ── CleaningTipsContent.jsx
             ── page.jsx
         ── commercial
             ── CommercialContent.jsx
             ── page.jsx
         ── faq
             ── FAQContent.jsx
             ── page.jsx
         ── gift-certificate
             ── GiftCertificateContent.jsx
             ── page.jsx
             ── success
         ── layout.jsx
         ── page.js
         ── privacy
             ── page.jsx
         ── residential-section
             ── page.jsx
             ── ResidentialContent.jsx
         ── service-quote
             ── page.jsx
             ── ServiceQuoteContent.jsx
         ── terms
             ── page.jsx
     ── admin
         ── appointments
             ── page.jsx
         ── customers
             ── page.jsx
         ── dashboard
             ── page.jsx
         ── invoices
             ── page.jsx
         ── layout.jsx
         ── notifications
             ── page.jsx
         ── reports
             ── page.jsx
         ── requests
             ── page.jsx
         ── send-welcome
             ── page.jsx
         ── settings
             ── page.jsx
     ── api
         ── admin
             ── approve-service-request
             ── create-appointment
             ── create-invoice
             ── customers
             ── decline-service-request
             ── delete-appointment
             ── get-all-appointments
             ── get-all-customers
             ── get-all-invoices
             ── get-service-requests
             ── invoices
             ── update-appointment
             ── update-invoice
         ── auth
             ── complete-invite
             ── validate-invite
             ── verify-handoff
         ── booking
             ── [id]
             ── route.js
         ── create-gift-checkout
             ── route.js
         ── cron
         ── customer-portal
             ── delete-account
             ── invoice
             ── service-requests
         ── email
             ── appointment-cancelled
             ── appointment-confirmed
             ── appointment-rescheduled
             ── customer-welcome
             ── notify-appointment-change
             ── service-request-declined
         ── notifications
             ── zelle-alert
         ── send-gift-certificate
             ── route.js
         ── stripe
             ── create-payment-intent
             ── create-setup-intent
             ── pay-stripe-invoice
             ── save-payment-method
         ── verify-recaptcha
             ── route.js
         ── webhooks
             ── stripe
     ── auth
         ── admin-invited-set-password
             ── page.tsx
         ── callback
             ── route.js
         ── forgot-password
             ── page.jsx
         ── login
             ── page.jsx
         ── profile-setup
             ── page.jsx
         ── reset-password
             ── page.jsx
         ── set-password
             ── page.jsx
         ── signup
             ── page.jsx
         ── verify-email
             ── page.jsx
     ── globals.css
     ── HomePage.jsx
     ── layout.js
     ── portal
         ── appointments
             ── page.jsx
         ── customer-feedback
             ── page.jsx
         ── dashboard
             ── page.jsx
         ── invoices
             ── [id]
             ── InvoiceSidePanel.jsx
             ── page.jsx
         ── layout.jsx
         ── notifications
             ── page.jsx
         ── request-service
             ── page.jsx
         ── service-history
             ── page.jsx
         ── settings
             ── page.jsx
         ── shared-animations.module.css
 ── components
     ── admin
         ── AdminNav.jsx
         ── AdminNotificationBell.jsx
     ── Breadcrumbs.jsx
     ── ContactButton.jsx
     ── Footer.jsx
     ── Header.jsx
     ── PageTransition.jsx
     ── portal
         ── PageTitle.jsx
         ── PortalNav.jsx
         ── RecentNotificationsCard.jsx
         ── SupportCard.jsx
     ── StaggerItem.jsx
     ── TawkToChat.jsx
     ── ui
         ── AddressAutocomplete.jsx
         ── Badge.jsx
         ── Button.jsx
         ── Card.jsx
         ── Input.jsx
         ── LoadingSpinner.jsx
         ── Modal.jsx
         ── PasswordInput.jsx
         ── SelectableCard.jsx
         ── SkeletonLoader.jsx
         ── Toast.jsx
         ── TurnstileWidget.jsx
 ── hooks
     ── useRecaptcha.js
 ── lib
     ── emailTemplate.js
     ── middleware.js
     ── rateLimit.js
     ── sanitize.js
     ── supabase
         ── admin.js
         ── client.js
         ── middleware.js
         ── server.js
 ── types
     ── supabase.ts

102 directories, 96 files
